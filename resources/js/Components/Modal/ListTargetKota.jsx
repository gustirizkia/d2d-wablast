import { Link } from "@inertiajs/react";
import React, { useState } from "react";

export default function ListTargetKota({ listKota, isShow = true }) {
    const [showModal, SetModalShow] = useState(isShow);

    return <></>;
}
